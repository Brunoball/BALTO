# Configuración exclusiva para `npm start` / desarrollo local.
# Sobrescribe únicamente el basename definido en .env de producción.
# Así http://localhost:3000/ abre el LOGIN directamente.
REACT_APP_ROUTER_BASENAME=/
